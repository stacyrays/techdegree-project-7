import React from "react";

const Home = () => {
  return (
    <div className="main-content">
      <h2>Search Flickr Images Now</h2>
      <p>
        React and React Router are being used to build this app. Also uses a
        Flickr API key and secret. Fetch is used to pull the data and it is
        being converted into JSON, and parsing out the image urls for the image
        listing page.
      </p>
    </div>
  );
};

export default Home;
